import { Deployment, DeploymentBuild } from './types';
interface DeploymentStatus {
    type: string;
    payload: Deployment | DeploymentBuild[];
}
export default function checkDeploymentStatus(deployment: Deployment, token: string, version: number | undefined, teamId: string | undefined, debug: Function): AsyncIterableIterator<DeploymentStatus>;
export {};
