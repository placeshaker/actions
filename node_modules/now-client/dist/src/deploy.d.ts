import { DeploymentFile } from './utils/hashes';
import { DeploymentOptions, NowJsonOptions } from './types';
export interface Options {
    metadata: DeploymentOptions;
    totalFiles: number;
    path: string | string[];
    token: string;
    teamId?: string;
    force?: boolean;
    isDirectory?: boolean;
    defaultName?: string;
    preflight?: boolean;
    debug?: boolean;
    nowConfig?: NowJsonOptions;
}
export default function deploy(files: Map<string, DeploymentFile>, options: Options): AsyncIterableIterator<{
    type: string;
    payload: any;
}>;
